# Booknest (Android)

Community book box app built with Jetpack Compose + Firebase.

## Server

- Google Books API (base URL): https://www.googleapis.com/books/v1/

## Local setup

1. Create `local.properties` (Android Studio generates it automatically).
2. Add the server base URL:

```
server="https://www.googleapis.com/books/v1/"
```
